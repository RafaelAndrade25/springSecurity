meta {
  name: User List
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
